---
# tasks file for demorole
- debug:
    var: "{{ app_name }}"
  register: "output"
- yum:
    name: "{{ item.name }}"
    state: "{{ item.state }}"
  loop: "{{ output }}"
- copy:
    content: "{{ item.content }}"
    dest: "{{ item.destination }}"
  loop: "{{ output }}"
- service:
    name: "{{ item.name }}"
    state: started
  loop: "{{ output }}"
